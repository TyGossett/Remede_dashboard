Initial Setup:

Download and install some Python compiler like the one found here https://www.python.org/
Ensure folder is reachable by compiler

using terminal, navigate to the remede_dashboard/venv/Scripts
activate virtual enviornment
install packages found in requirements doc using the pip install command